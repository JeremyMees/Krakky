import { ContainsNumberDirective } from './contains-number.directive';

describe('ContainsNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainsNumberDirective();
    expect(directive).toBeTruthy();
  });
});
